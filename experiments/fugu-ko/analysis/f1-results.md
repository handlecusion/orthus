# F1 결과 — 위임추출 k=3 자기일관성 글루 (2026-07-23)

> 사전선언 `f1-delegation-sc-prereg.md`(구현·측정 전 커밋 `04a9af0f`).
> 구현: `orthus/agentwork/delegation.py` — `ORTHUS_DELEGATION_SC_K`(default 1 = 현행 byte-identical,
> fail-closed), 결정론 엄격-과반 집계. prod 활성화는 owner 결정 대기.

## 등록 예측 채점

| 예측 | 결과 |
|---|---|
| ① flow 72→74/84 이상 | **부분 적중**: 73/84(86.9%) — +1(B-g3-0011 flaky miss 구제), 국내 단일과 동수. Sonnet(74)에는 −1: 잔여 B-g3-0012/0013은 k=3에서도 재현되는 **일관 실패**라 다수결로 구제 불가 |
| ② 파손 0 | **적중** — 기존 pass 플로우 전부 유지 |
| ③ 오탐 악화 없음 | **강하게 적중**: 동일 기준 쌍대 대조에서 k=1 오탐 7/24(2회 재현) → k=3 **4/24 (−43%)**, 미검출 0/8 유지. 자기일관성이 flaky miss와 one-off FP를 동시에 억제 |

## 해석

- 오케스트레이터 flow 결손의 유일한 "고칠 수 있는" 부분(벤더 재실행 편차)은 글루로 닫혔다.
  잔여 Sonnet 격차 1건은 EXAONE의 일관 오추출 — 모델 교체 또는 프롬프트 개선 영역.
- 위임은 안전 임계 슬롯(오탐 = 팀원 PC full-access 에이전트 실행)이라 FP −43%가 flow +1보다
  중요한 수확일 수 있다. 비용은 위임추출 콜만 3배(전체 flow LLM 콜 58→66, +14%).
- 측정 함정 재발 기록: 첫 SC arm이 ORTHUS_LLM=mock 함정(§8.4 5호)으로 무효 — llm_calls=0으로
  즉시 적발, ORTHUS_LLM=solar로 재실행한 것이 본 결과다.

기존 "EXAONE 오탐 2" 기록과의 차이는 집계 기준(실존 팀원 명명 트랩만 vs 전 트랩 assignee-비공백)
차이다. 본 문서의 7→4는 동일 스크립트·동일 기준 쌍대 비교라 내적으로 유효하다.
