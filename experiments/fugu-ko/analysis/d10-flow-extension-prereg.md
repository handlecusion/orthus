# D10 — Flow Bench 비례 확장 (84 → 168) 사전선언 (2026-07-24)

> 상태 **등록(저작·측정 전)** · owner 지시 "⑥ 문항수 168으로". D9(145→200)와 동일 규율 상속:
> 블라인드 저작 · 기존 문항 byte-identical · mock 불변식 선검증 · 등록 예측 · 무효원칙.

## 1. 구성 (채점가능 문항 기준 정확 2배, 합 +84)

| flow | 기존 채점 | 신규 | 합계 | 내용 |
|---|---|---|---|---|
| g1 문서초안→발행 | 3 | +3 | 6 | structural만(judge류 저작 금지) |
| g2 세션→위키/라우팅 | 42 | +42 | 84 | prebuilt/agent_authored 비율 유지, ambiguous_route 축 포함 |
| g3 메일→회신/위임 | 20 | +20 | 40 | delegation 상태 어설션 포함 |
| g4 위임→회수 | 19 | +19 | 38 | 결정론 게이트 어설션 |
| **계** | **84** | **+84** | **168** |

## 2. 저작 규율

1. 기존 g*.jsonl 스키마·fixture 관례·태그 체계 그대로. 신규 id `B-g{n}-1xxx`, 태그 `d10_ext`.
   fixture는 기존 fixture 파일 재사용 또는 신규 fixture(sha256 동결) — 모델 응답 참조 금지,
   특정 모델이 틀린 문항의 변형 금지(비례 확장의 정의).
2. manifest 재빌드 후 **기존 137 flow 라인 byte-identical** 증명. mock 런(--only-tag d10_ext)
   에서 model-independent FAILS = 0 확인 후에만 동결.
3. 채점 어설션은 결정론 상태-기반만(SWE-bench 방식 유지) — judge류 신설 금지.

## 3. 측정 계획

신규 84만 전 arm 실측(--only-tag d10_ext, staging DB 직렬), 기존 84 결과 재사용해 합산 n=168.
arm: 오케스트레이터(F1+F2b+F3 현행 배정) · Sonnet · Opus 4.6/4.8 · solar · exaone ·
gpt-5.3 · gpt-5.5†/5.6†(codex) · DSv4. GLM은 레이트 캡 해제 확인 시에만.

## 4. 등록 예측

1. 오케스트레이터+글루가 n=168에서도 **1위 유지**(신규 문항에서 F2b 어휘 게이트가 신규
   ambiguous_route를 같은 방식으로 구제).
2. 프론티어(Sonnet/Opus)와 국내 단일의 격차 구조 재현: 신규 g2 라우팅 실패는 전 모델 공통.
3. 파손 0: 기존 84 결과는 동결 재사용이라 불변(구조적).
틀리면 그대로 보고한다 — 특히 신규 문항에서 글루 이득이 재현 안 되면 "기존 84 과적합" 증거로 기록.

## 5. 비용

신규 84 × 10 arm ≈ 840 플로우 실행(플로우당 LLM 0-3콜) ≈ LLM ~1,300콜. Bedrock 3 arm ~400콜.
