# F4a — email 초안 프롬프트 구조화 사전선언 (2026-07-24)

> 상태 **등록(변경·측정 전)** · owner 지시 "생성 품질 개선 — F4a부터". 대상은 프로덕션
> `orthus/agentwork/service.py::_generate_command_email`의 system 프롬프트(아레나 email 배틀이
> 이 실함수를 그대로 구동 — arena_gen.py).

## 1. 근거 (기측정)

- 아레나 email: solar decided 승률 32%(n.s.)·tie 43% — 격차가 생성 축 유일 잔여.
- 생성물 통계: **Sonnet 451자 vs solar 336자**(t2에선 길이 차 없음·tie 90%) — 판정 격차가
  완결성/구조(수신 맥락, 액션 아이템, 맺음) 차이일 가능성. t2는 개선 불가 구간(tie 90%)이라
  email에 집중한다.

## 2. 변경 (프롬프트 = 결정론 글루)

system 프롬프트에 구조 요구를 추가: ① 수신인 맥락을 여는 문장 1줄 ② 본문은 요청·참고자료의
구체 내용으로 ③ 필요한 경우 액션 아이템/기한을 명시적 목록으로 ④ 정중한 맺음과 서명 자리.
사실 지어내기 금지·빈칸 대괄호 규칙·JSON-only 출력 계약은 불변. 코드 경로·가드 변경 없음.

## 3. 측정 계획

1. solar로 email 30문항 재생성(arena_gen, 프로덕션 함수 경유 — F4a 프롬프트).
2. **주판정**: 신규 solar-F4a vs claude-sonnet-4-6(기존 생성물) — 검증 판정자 gpt-4o,
   위치스왑 60표, 기존 프로토콜 그대로.
3. **부판정(A/B)**: solar-F4a vs solar-구프롬프트 직접 쌍대 60표 — 개선분 자체를 분리 측정.
4. 회귀: 층2/flow는 email 생성 품질을 채점하지 않으므로 구조적 무영향(구조 어설션이 있는
   flow g2 이메일 커맨드 항목은 subject/body 존재만 봄) — flow 재실행은 생략하되 명기.

## 4. 등록 예측

1. vs Sonnet decided 승률 32% → **45% 이상** (동률권 진입 방향).
2. A/B에서 F4a가 구프롬프트에 유의 우세(승률 >50%, p<0.05).
3. JSON 파싱 실패 0 유지·폴백 0 유지.
틀리면 그대로 보고하고 F4b(best-of-2)로 넘어간다.

## 5. 확장 등록 (2026-07-24, 측정 전) — 프론티어 다면 배틀

owner 지시: F4a를 Sonnet 한정이 아니라 프론티어 다수와 배틀. 대상 생성물: 기존 블라인드
생성 Opus 4.5·4.6(기보유) + 신규 생성 Opus 4.8·gpt-5.3(같은 프로덕션 함수·같은 문항 30).
판정은 동일 검증 판정자(gpt-4o)·위치스왑·같은 드라이버. **예측**: F4a가 각 프론티어 상대
decided 승률 50% 이상(무패까지는 예측하지 않음 — Sonnet 결과의 일반화 시험).

## 6. 스코어화 등록 (2026-07-24, 측정 전) — 라운드로빈 아레나 점수

owner 지시: 배틀 나열 대신 모델별 단일 점수. **8모델 풀 라운드로빈**(F4a·solar-구·exaone-구·
Sonnet·Opus 4.5/4.6/4.8·gpt-5.3, C(8,2)=28쌍 × 30문항 × 위치스왑 2 = 1,680표, 검증 판정자
gpt-4o) 후 **평균 승점 = (승 + 0.5×무) / 전투수 × 100**을 모델 점수로 한다(Elo는 tie 다수
구간이라 채택 안 함 — arena-results §4 결론 상속). 기측정 쌍(F4a vs 6종 등)은 재사용하고
미측정 쌍만 신규 판정. 예측: F4a 1위, 순위가 기존 쌍대 결과와 무모순.
