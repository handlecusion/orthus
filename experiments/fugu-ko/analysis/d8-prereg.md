# D8 사전선언 — 학습기 재도전 (생성기 결함 수정판)

> 상태 **LOCKED 2026-07-17** (판정셋 생성 완료·워커 캡처 시작 전 잠금). 이후 수정 금지.
> D7 prereg(`d7-prereg.md`)의 §1(무효원칙)·§6(판정기준)·§7(다중비교)·§8(반박문답)·§9(패배문안)를
> 그대로 상속하고, 아래만 갱신한다.

## 1. D8이 존재하는 이유 (D7과의 차이)

D7 본판정은 무승부(c2 81.8 vs R+ 83.5, 양측 p=0.25)였으나, 사후 부검(E12)에서 **판정셋
생성기 결함**이 발견됐다: 질문의 26.9%에 조사 마커(`[에]` 등)가 미해결 잔존 + 합성값에
필드명 접두 — 학습기의 복사-규율을 편파적으로 벌주는 결함(클린 슬라이스에선 c2 83.9 vs
R+ 81.2, 단측 p=0.061). **결함을 고친 시험지에서 딱 한 번 다시 겨룬다.** 이는 결과가 마음에
안 들어서가 아니라 시험지가 깨져 있었기 때문이다 — 결함 증거는 E12에 판정 전 수치와 함께
보존돼 있다.

## 2. 판정셋 (생성 완료 — 캡처 전 잠금)

`golden/t3_d8_holdout.json` — **1,438문항**, 신규 합성 DB **12종**(명시; 학습 6·구홀드아웃 6·
D7 fresh 10과 무겹침; 끝공백 3종·이모지 4종 등 워크스페이스 버릇 반영), 신규 템플릿 17종
(gen2·D7 fresh·v4 증강과 문자열 중복 0 assert), zero 108(8%), gold 결정론 실행 산출.
**생성기 수정 2종:** 조사 마커를 변수 뒤에 두지 않음(잔존 0 assert) + 합성값 필드명 접두 금지.
distinct-spec 위주(패러프레이즈 ≤3~4)로 클러스터 상관을 낮춤.

## 3. 대결 시스템 (동결)

```text
공통 글루(양쪽 동등 제공 — 공정):
  snap      db_name이 카탈로그에 없으면 공백무시 유일일치로 교체
  probe-0   최종 후보가 null(에러|빈|{0})이고 채택 SQL의 모든 필터값이 해당 컬럼에
            SELECT EXISTS로 부재 확인되면 최종 답 {0} 확정 (D7 R+의 zero 구멍 봉합)
  폴백 체인에서 첫 non-null 채택, 전부 null이면 1차 답 유지

R+ (규칙기) = solar k=5 자기일관성(t0+4×t0.7, null제외 다수결) → snap → 결정론 수리 2종
              (repair.py) → probe-0 → null 폴백(ax → exaone)
c8 (학습기) = SFT 워커 k=5 자기일관성(greedy+4×t0.7, 실행결과 null제외 다수결) → snap →
              probe-0 → null 폴백(solar t0 → ax)
  주판정 백본 = sft32b_v4 (EXAONE-4.0-32B QLoRA, v4 데이터).
  단, 학습셋 val에서 sft12b_v4가 더 좋으면 그걸 주판정으로 지정(판정 전 val로만 선택·동결).
  나머지 백본은 ablation 보고 전용 — 판정셋에서 "좋은 쪽 고르기" 금지(§7 상속).
```

## 4. 판정 기준 (D7 §6 상속 — 동일)

McNemar p<0.05(c8>R+) ∧ 부트스트랩 10k P(c8>R+)>95% ∧ zero 슬라이스 비열등 ∧ 단일 답.
헤드룸 게이트는 **폐지**(D7에서 R+>oracle로 무의미 판명 — 결함 기록 E11) — 대신 R0~R+
사다리·oracle 전부 병기 보고. 승리 시 **독립 재현셋**(다른 seed·다른 합성 DB 구성)에서
1회 재현해야 최종. 실패 시 D7 §9 문안으로 보고.

## 5. 실행 순서

① 워커 캡처(solar k5+ax+exaone, SQL 포함) → ② v4 학습(32B QLoRA·1.2B, A100) + 학습셋 val로
주판정 백본 동결 → ③ c8 생성(k=5) → ④ `judge_d8.py` 1회 실행 → ⑤ 승리 시 재현셋.
캡처·생성은 판정 정의와 독립이므로 병렬 진행 가능. 판정은 ①~③ 완료 후 1회.
