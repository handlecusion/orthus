# F3 — 생성 슬롯(wiki_qa·email_draft) solar 재배정 (2026-07-24)

> 상태 **등록 → 즉시 실행**(신규 측정 아님 — 기측정 증거 기반 배정 변경). owner 지시 "⑤ 개선".

## 1. 근거 (전부 기측정)

- **아레나 3중 누적**: exaone 생성은 Sonnet(검증 판정자 gpt-4o, p=.022/.0015)·Opus 4.5·4.6
  상대로 전부 유의 열세. **solar는 3 프론티어 전부와 동률**(email은 동률~우세 방향, tie 63/37%).
- §12 기존 실측: wiki_qa 근거 판정 Solar 12–0 A.X·EXAONE과 90% 무승부, email_draft
  Solar 5/30 vs EXAONE 4/30 **p=1.000** — 재배정이 다른 품질 축을 해치지 않는다.
- §15의 EXAONE 배정은 "동점 구간 안 다양화" 선택이었다 — 아레나가 동점을 깼으므로
  같은 §15 원칙(동점 구간 내 선택) 아래서 증거 방향으로 되돌린다.

## 2. 변경

`ASSIGNMENTS`: `wiki_qa`·`email_draft` → **solar** (였던 exaone). 위임(t10)·라우팅(t5) 등
타 슬롯 불변. 문서 SoR(`docs/model-orchestration.md` §15)에 개정 기록.

## 3. 등록 예측 (실행 전 고정)

1. **아레나 조립 성적 = solar 성적으로 동률화** — 신규 콜 없이 성립(같은 모델·같은 프롬프트;
   solar arm이 이미 블라인드로 측정돼 있음). 카드에는 "이전 배정(exaone)의 열세 기록"을 보존.
2. **층2 n=200 불변** — wiki_qa/email_draft 슬롯은 층2 6작업 composite에 미사용(구조적).
3. **Flow 무손상** — 재배정+F1+F2b 오케스트레이터 flow 재실행에서 82/84 유지(생성 슬롯이
   플로우 채점에 개입하는 경로 확인 포함). 깨지면 그대로 보고하고 재배정을 재검토한다.
