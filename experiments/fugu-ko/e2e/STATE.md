# E2E Benchmark — Orchestration State

Plan: `~/.claude/plans/mossy-nibbling-nebula.md`
Repo: `<repo>`
Working dir: `experiments/fugu-ko/e2e/`
Last updated: 2026-07-21 (Phase 5+6 execution complete except Bedrock; e2e_report.md fully written incl. Phase 6 §6.0-6.4; remaining: t2/t8 judge eval, Bedrock 5-model round, commit)

## Hard gates (NEVER violate)
- User-direct-approval gate for the large models was REPLACED by an orchestrator dataset audit (2026-07-21, user directive) — see GATE line below for verdict. Phase 6 execution has proceeded under that verdict for GPT-4o/DeepSeek/GLM-5.2; Bedrock 5 (Claude Sonnet 4.6, Claude Haiku 4.5, Llama 3.3 70B, Llama 3.1 8B, Nova Pro — DeepSeek moved OFF Bedrock, see Phase 6 line) remain unexecuted because the key itself returns 403 (invalid), not because of a pending approval gate. Only reachable via harness `--final-verify`.
- GPT-4o-mini = baseline comparison only, never a production slot.
- Bedrock token expires 2026-07-31. GPT-4o/DeepSeek/GLM no expiry (DeepSeek's own `deepseek-chat` deprecation on 2026-07-24 is now moot — already executed via official API, see Phase 6 line).
- No API key VALUES in source/commit/PR/chat — env names only (ORTHUS_LLM_BEDROCK_API_KEY etc.).
- Invariants asserted every run: `model.fallback == 0`; no emoji-DB "confident-zero".
- Results are measured, not designed — no target ranking pattern (§6b hard gate 4).

## Phase status
- [x] Phase 0: inventory.json + manifest_schema.md  (2026-07-20)
- [x] Phase 1: Tier A -> e2e/tier_a.jsonl (279 items, validated)  (2026-07-20)
- [x] Phase 2: L2 gaps g1-g4 -> e2e/l2/{g1..g4}.jsonl (137 items, validated)  (2026-07-20)
- [x] Phase 3: Tier B -> e2e/tier_b.jsonl (1653 L1) + freeze.lock  (2026-07-20)
- [x] Phase 4: harness_e2e.py + offline mock smoke PASS (live canary pending keys)  (2026-07-20)
- [x] Live canary (solar/exaone/ax/baseline, `--limit 3`): PASS — 0 fallback, negative gate clean  (2026-07-21)
- [x] Phase 5: domestic live bench (distill EXCLUDED — cite existing T14) + stats + evidence pkg — **COMPLETE, corrected** (2026-07-21). Two defects found and fixed mid-run: (a) first full run targeted an empty `orthus` DB (both `ORTHUS_PG_DSN` and `ORTHUS_PG_DSN_READONLY` must be overridden — RO DSN is hardcoded to `orthus` in `.env`; staging-named DBs are auto-TRUNCATEd by the harness — correct target is `orthus_company` live read-only) faking 18 t3 exact failures across all models; rerun in `analysis/raw/t3_rerun_orthus_company/`, empty-DB full-run backup in `analysis/raw/phase5_full_orthus_db/` (not used for stats). (b) t10 scorer honorific bug (assignee "님" broke exact-match) fixed via `_strip_honorific` in `harness_e2e.py`; offline rescore recovered EXAONE +6 (15/22 -> 21/22), no other model affected. Final corrected result (scored n=145): EXAONE 81.38% > baseline 79.31% > Solar 77.24% > A.X 75.17%; only significant pair EXAONE>A.X p=0.049 (borderline). Data: `analysis/raw/phase5_final_stats.json`.
- [x] GATE: user-direct-approval gate REPLACED by orchestrator dataset audit (user directive, 2026-07-21). Verdict: **PROCEED-WITH-CAVEATS** (no integrity defects; caveats: (1) 75% of executed items are t3 structured -> report MUST break down by task, (2) 62 excluded pending items are the model-discriminating L2 set -> current L2 run is pipeline-validation-only, (3) 24 anchors are unverified by a human despite the `human_verified` tag, (4, added 2026-07-21) GLM-5.2 executed Tier-A-only for budget reasons -> GLM's row in the 7-model comparison has different tier coverage than the other 6). Full rationale was in a since-evicted scratchpad `dataset_audit.md`; the caveats above are the durable summary.
- [x] Phase 6: final verify — **Bedrock EXCLUDED this round** (key returns 403, judged invalid, user is following up with issuer; still unresolved). GPT-4o + DeepSeek + GLM-5.2 executed against `orthus_company` (both DSNs) with the corrected t10 scorer. GPT-4o full run complete (26min, log `analysis/raw/e2e_phase6_live.log`). DeepSeek via official API (NOT Bedrock — confirmed absent from team Bedrock account) using the `deepseek` slug wired into `harness_e2e.py`; full run complete, log `analysis/raw/e2e_phase6_deepseek.log` (the 2026-07-24 `deepseek-chat` deprecation deadline is now moot). GLM-5.2 hit a billing incident (~$10 burned mid-run on reasoning-token overage before realizing full-run cost would hit $30-40) -> user approved scope-down to Tier A only (~35min); a sequential-execution bottleneck (throughput fell to ~40/hr partway through t3's 477 items) was also found and fixed by splitting into a parallel run; log `analysis/raw/e2e_phase6_glm.log`. 7-model common-scored comparison (n=145): DeepSeek 83.45% > EXAONE 81.38% > baseline=GLM 79.31% (tie) > GPT-4o 78.62% > Solar 77.24% > A.X 75.17%; DeepSeek significantly beats everyone except EXAONE (tie, p=0.45); GPT-4o vs baseline also ties (p=1.000); all 30-60 per-model errors are 429 rate-limits with zero overlap with the scored set. Data: `analysis/raw/phase6_verified_stats.json`. Remaining: Bedrock 5-model round once user confirms the key fix (DeepSeek no longer part of that round).

## Phase 0 outputs & carried-forward flags
- inventory.json: 34 golden assets. Tier A=11, Tier B=14 (incl. 3 draft supersets marked reexecute=false), reused_concluded=9.
- manifest_schema.md (544 lines) = AUTHORITY on per-item record format. L1 task codes t2..t16 (t15/t16 mint for the 3-way generation split); L2 codes g1-g4. expected.kind in {exact, metric, judge, structural}. frozen.input_sha256 = sha256 of sorted-keys/no-whitespace/ensure_ascii=False/UTF-8 JSON of `input`.
- Flags:
  - t10_holdout2.json scorer (t10_delegation.py) hardcodes golden/t10_delegation.json — needs a --golden override to re-execute.
  - routing_holdout.json / routing_graph_golden.json conclusions live in docs/routing-holdout-plan.md & docs/routing-graph-follow-up.md (NOT analysis/).
  - e6_trap.json conclusion doc inferred (analysis/svc-cascade-verify-results.md).
  - e3_control/e3_missed = Tier A with dual role; independent-holdout conclusion in docs/decompose-prefilter-ext.md.
  - item counts taken from actual items/main/control array lengths, not top-level dict keys.

## Phase 1 outputs & flags
- tier_a.jsonl: 279 items = sum of 11 Tier-A asset counts. Per-task: t2=30, t3=28, t5=21, t6=20, t7=118 (t7 base 22 + t7_holdout 16 + e3_control 40 + e3_missed 40), t8=8, t9=32, t10=22. `build_manifest.py` is the reproducible converter.
- Judge-scored: t2, t8 (rubric evidence-pairwise-winrate). Rest deterministic (expected.kind=exact).
- t3 ground truth recomputed live vs orthus_company DB: 18/28 have a verified intset (emoji-db t3-10 -> [19]); 10 are gate_only per t3_gold.py exclusion list.
- FLAGS to honor in the harness: (1) e3_control/e3_missed/t7_holdout minted under task code t7 (schema has no e3 code); identity kept in `tags` (e3/missed_probe/control_probe + aggregate_scored_set_wide). Harness must dispatch these to e3_prefilter scoring BY TAG, not generic t7. (2) t8 items carry only the parent question; synthesize() needs frozen sub_answers/sub_questions (tag requires_frozen_subs) -> t8 stays qualitative (n=8, §11). (3) t10 golden lacks instruction field (tag no_instruction_field_in_golden). (4) t7_holdout provenance set to golden inside Tier A even though inventory labels it independent_holdout.

## Phase 2 outputs & flags

- L2 complete: 137 items across e2e/l2/{g1=40,g2=42,g3=36,g4=19}.jsonl + e2e/fixtures/g{1..4}/ (75 fixtures) + e2e/l2/USER_FILL_CHECKLIST.md (master, 316L) + per-flow g*_USER_FILL.md + e2e/l2/DESIGN.md.
- Validated: 137 unique ids (no collision), all required keys, Tier A=81/B=56, pending_user_fill=62 (== frozen-null == the 62 model-discriminating), fully-built=75, anchors=24.
- MANIFEST STRUCTURE: full manifest = tier_a.jsonl (L1 Tier A, 279) + l2/g*.jsonl (ALL L2, both tiers by flow) + tier_b.jsonl (L1 Tier B only, Phase 3). Harness combines all three; the `tier` field distinguishes A/B. L2 items are NOT duplicated into tier_b.jsonl.
- 62 pending stubs need USER fill (corpus-sample-user-run / synthetic-blind-user-authored / use-existing-golden). g3-X fastest path = existing golden/t10_holdout2.json h-01/03/07/15/27/30. 24 anchors need human-verified expected values.
- DESIGN.md corrections applied in jsonl (jsonl is authoritative): g4 reject=HTTP200+state=rejected (NOT 422), empty-instruction=pre-gate 422; g3 approve-twice=409 (NOT 200 already_sent), delegation trap h-09->h-30 (new mail/delegation_prefilter.py); g1 multi-doc merge=single-call preseeded; g2 session fixture split x4.
- Minor (documented in USER_FILL_CHECKLIST.md §C): g4 anchors tagged `human_verified` (no _anchor suffix) vs g1/g2/g3 `human_verified_anchor` -> treat both as anchors. g3_USER_FILL.md table omitted A-g3-0013 (present in jsonl + checklist).

## Phase 3 outputs & flags
- tier_b.jsonl: 1,653 L1 independent-holdout items. t2=30, t3=1483 (t3_holdout 41 + t3_v2 4 + t3_d8 1438), t5=88 (t5_holdout 28 + t5_v2 20 + routing_holdout_tn 40), t6=20, t10=32. All provenance=independent_holdout, B- prefix, disjoint from L2 B-g* ids. build_tier_b.py = reproducible converter. FULL MANIFEST = tier_a 279 + l2 137 + tier_b 1653 = 2069 items (62 pending).
- freeze.lock: tier_a count=350 (279 + 71 built L2-A), sha256 c1249d04...; tier_b count=1657 (1653 + 4 built L2-g4-B), sha256 b2833269...; 62 pending stubs excluded+counted (A=10, B=52). This is the GATE drift hash.
- t3_d8 gold EMBEDDED -> frozen (108 genuine-zero -> result [0]/[] + no_confident_zero); reuses §1b D10 LOCK n=1438 p=5.6e-187. t3_holdout gold recomputed LIVE vs orthus_company (41 verified; DB REACHABLE). t3_unseen(463)/t3_fresh(1063) NOT wrapped = supplementary sample-on-demand (d8 adequate).
- routing_holdout_tn -> task t5 (schema has no routing code); FP/genuine-zero via tags. t10 items tagged scorer_golden_override_required + golden_file_t10_holdout2 (harness must run t10_delegation.py with --golden t10_holdout2).
- POWER table: t3 assembled (Solar 98.8 vs gpt 97.2, +1.6pp) target ~1144, have 1438 -> POWERED. UNDERPOWERED (flag in evidence doc): t5 routing (n=48, MDE~14pp), t6 intent (n=20), t2 wiki_qa (n=30 judge), t3_v2 (n=4), t8 (n=8, qualitative-only). Safety metrics (non-McNemar upper-bound): routing_tn 0FP/40 <=7.5%, t10 0FP/24 <=12.5%. decompose cited from §1b (n=160, tie, p=0.80), not wrapped.
- HYGIENE: decompose few-shot 0 hits; blind holdouts 0 leaks into orthus/router|wiki|mail source; E3 token populations unchanged. ONE flagged overlap: B-t5-0048 (t5_v2 real-log anchor) == Tier A A-t5-0008/A-t7-0075 -> acknowledged NON-blind prod-log anchor (common wiki-routing Q), not a blind-holdout breach. 4 intentional within-tier_b 0706 real-log anchors reused as both t3-count and t5-route (different ids/tasks, no id collision).

## Phase 4 outputs & flags
- harness_e2e.py + e2e/runner_lib.py complete. Loads tier_a + tier_b + l2/g1..g4; skips+counts pending_user_fill; L1 in-process chat_model= injection; L2 fixture->scratch orthus_test DB (live-reflection FK topo-sort, "recent" timestamp sentinel, HMAC/Bearer signing for /mail/ingest) -> TestClient; e3/probe items routed to aggregate_e3 set-wide scoring (never generic t7); t8 qualitative-only; model-swap via monkeypatch for L2; McNemar/power/bootstrap-CI(10k) in runner_lib + pairwise_model_stats wired into summary; raw JSONL -> analysis/raw/ (gitignored); ORTHUS_ASK_SEMANTIC_CACHE_ENABLED=false at startup; invariants (model.fallback==0 audit-span delta + confident-zero) enforced, nonzero exit on violation. --final-verify gate fail-closed: bedrock:*/openai:gpt-4o*/glm:* refuse without the flag; only solar/exaone/ax/baseline/mock otherwise.
- OFFLINE MOCK SMOKE (mock LLM/embedding, orchestration off, orthus_test): 350 loaded, 62 pending skipped+logged, 0 hash mismatch, 0 errors, invariants clean, raw+summary written, reproducible x3. 60 deterministic PASS (incl. g4 15/15 branch coverage, g1 structural 3/3), 282 deferred-to-live (judge/metric/live-DB/t8/e3-aggregate/LLM-reaching).
- ⚠️ 8 smoke fails TRIAGED (e2e/SMOKE_FAIL_TRIAGE.md, harness miswire ruled out for all 8 by direct prod-function repro): (a) 2 PRODUCTION REGRESSIONS — A-t6-0008 (_BOARD_TERMS checked before _WIKI_TERMS in agentwork/service.py::_detect_command_family, misroutes "위키 태스크 정리해줘" contradicting the same file's own LLM prompt canonical example; unambiguous, actionable) + A-t5-0016 (_WIKI_TERMS intercepts relational question before documented LLM graph fallback). (b) 6 t7 STALE_GOLDEN — t7.json authored 2026-07-08, predates O4/E3 prefilter ext (docs/decompose-prefilter-ext.md 2026-07-13); prod default ORTHUS_DECOMPOSE_PREFILTER_EXT_TIER=0 means harness measured real prod behavior; A-t7-0005 additionally fails at EVERY tier incl. max (comma-adjacent "와," not covered by space-anchored T3 token) = residual gap beyond documented fix. DECISION: benchmark measures prod as-is — do NOT fix prod code mid-benchmark; findings go in competition evidence (benchmark caught real defects = e2e validity proof); user decides fixes at gate.
- Live canary (--limit 3, real keys) still pending (user action; local or A100 box per REMOTE_GPU_RUNBOOK.md). HARDENED (2026-07-20): DSN truncate guard (test/staging only; live DB -> warn + skip L2 fixtures, L1 read-only proceeds) + Phase 6 pool wiring (bedrock:/openai:/glm: adapters constructed only after --final-verify gate; missing key -> clean env-var-named error, no network). Mock smoke re-verified byte-equivalent after hardening.
- `.env`-based key config added (2026-07-20, `pool.py::build_pool`): solar/ax/exaone now fall back to repo-root `.env` (same `ORTHUS_LLM_SOLAR_API_KEY`/`_AX_`/`_EXAONE_` vars production reads via `orthus.models.registry.vendor_specs()`) when keys.json/`FUGU_KEYS` has no real key for that slug — keys.json still wins if present (no regression to existing keys.json-only callers, incl. `harness.py`). Also hardened `keys.json` itself into `experiments/fugu-ko/.gitignore` (`keys.json`/`**/keys.json`, defense in depth — previously only off-repo-by-convention). See `LIVE_BENCH_RUNBOOK.md` §1.1b.
- `.env` loading made uniform across ALL harness-read vars, domestic + Phase-6 large-model (2026-07-20, `harness_e2e.py` module top): the three Phase-6 pool builders (`_build_bedrock_chat`/`_build_openai_chat`/`_build_glm_chat`) read raw `os.environ.get(...)` with no dotenv loading of their own, unlike the domestic path (`pool.py::build_pool` -> `orthus.settings.get_settings()`, pydantic-settings' own `env_file=".env"`) — a `.env`-only large-model key was silently ignored without a manual `set -a && source .env` first. Fixed by calling `load_dotenv(_REPO_ROOT / ".env", override=False)` at harness_e2e.py's module top, `_REPO_ROOT` resolved via `Path(__file__).resolve()` (not CWD-relative), before any env var is read — this ALSO fixes the pre-existing CWD-relative-loading gap in the domestic path, since by the time `orthus.settings.get_settings()` does its own (CWD-relative) `env_file` lookup, the real value from `.env` is already in `os.environ` and wins regardless of process CWD. `override=False` keeps real shell-exported vars winning over `.env` (no behavior change for existing export-based users). `python-dotenv` is not a new dependency — already a hard transitive dep of `pydantic-settings` (top-level `orthus` dep), so `pyproject.toml` is unchanged. Verified with dry (non-real) `.env`-only values from an arbitrary CWD with no shell export: all three `_build_*` construct cleanly (no network call); domestic `get_settings()` also confirmed CWD-independent. Offline mock smoke re-verified byte-identical after the change (60 pass / 8 known-triaged fail / 62 pending / invariants clean). See `LIVE_BENCH_RUNBOOK.md` §1.1b + §5.

## Remote execution decision (user, 2026-07-20)
- Phase 5+ live bench may run on the user's A100 80G box (tailscale-reachable). GPU is IRRELEVANT (all models are remote APIs) — the box is just a roomy Linux host (user's local machine is low on disk).
- DB policy (user-decided): company DB stays LOCAL; the A100 box reads orthus_company READ-ONLY via tailscale/SSH tunnel (no company-data copy to the box). Scratch orthus_test/staging Postgres runs ON the box (docker).
- Vendor keys: injected as shell env on the box only (no files/commits).
- e2e/REMOTE_GPU_RUNBOOK.md written (tailscale tunnel read-only; scratch PG on box; guard denies orthus_company truncate).

## Resolved TODO (2026-07-21)
- DeepSeek V3.2 Bedrock modelId CONFIRMED (was open TODO in plan §11): `deepseek.v3.2`. **SUPERSEDED same day**: user confirmed DeepSeek is NOT actually in the team's Bedrock account, so this modelId was never callable — DeepSeek was instead wired as a new `deepseek` slug in `harness_e2e.py` hitting the official DeepSeek API directly (`ORTHUS_LLM_DEEPSEEK_API_KEY`/`_MODEL`/`_BASE_URL`), and Phase 6 already executed DeepSeek that way (see Phase 6 line above). The Bedrock 5-model remaining round no longer includes DeepSeek.

## Delegation model
- Orchestrator delegates ALL reads/writes to subagents; each returns <=10 sentence summary; large outputs summarized inside the subagent. Orchestrator holds conclusions only, keeps context fresh, mindful of 5h session reset.
- Tiers (user-updated 2026-07-20): sonnet = default for all delegation (incl. file wrap/format/grep); opus = complex work only (bias-avoidance design/judge prompts/stat power/evidence synthesis). haiku NOT used.
- Only the orchestrator edits STATE.md.
